import React from "react";
import "../styles/hero.css"; // Import af komponentens tilknyttede styling

// Funktionel komponent, der definerer hero-sektionen på forsiden
const Hero = () => {
  return (
    <section className="hero">
      {/* Hero-sektionen fungerer som visuel introduktion til sitet */}
      {/* Tekst eller call-to-action kan evt. tilføjes som overlay */}
    </section>
  );
};

export default Hero;
